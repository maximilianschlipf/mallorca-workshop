server:
  address: 127.0.0.1
  port: 18081

spring:
  jackson:
    deserialization:
      fail-on-unknown-properties: true
  datasource:
    url: jdbc:h2:mem:schulungsplaner;DB_CLOSE_DELAY=-1;MODE=PostgreSQL

app:
  demo-seed: true

schulungsplaner:
  katalog:
    pfad: ../katalog
    commit:
      name: Schulungsplaner Test
      email: schulungsplaner-test@localhost
