server:
  address: 127.0.0.1
  port: 18081

spring:
  datasource:
    url: jdbc:h2:mem:schulungsplaner;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: false
      path: /h2-console
